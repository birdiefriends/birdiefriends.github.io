# Deploy patch test file

This is a throwaway test artifact created to verify that `bf_deploy.py`'s patched
`deploy_file()` can create a brand-new file in the repo (not just update an existing one).

Safe to delete once confirmed.

Updated once to confirm the existing-file (sha-included) path still works unchanged.
